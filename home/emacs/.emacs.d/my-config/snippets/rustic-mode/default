# -*- mode: snippet -*-
# name: Implement `Default` function for Rust
# key: default
# --

impl Default for $1 {
    fn default() -> Self {
        Self {
            $0
        }
    }
}
impl $1 {
    /// Construct a new instance, same as \`Self::default()\`
    pub fn new() -> Self {
        Self::default()
    }
}