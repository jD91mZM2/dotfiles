# Bootstrapping my server

My VPS is provisioned with terraform. This is a very simple terraform script to
create a NixOS-based image. The image is later updated using
[morph](https://github.com/DBCDK/morph).
